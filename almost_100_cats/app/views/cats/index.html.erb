<h1>Mess O' Cats</h1>
<a href = "<%= new_cat_url %>">Put a new cat in here</a>
<br>
<ul>
  <% @cats.each do |cat| %>
  <li>
    <a href = "<%= cat_url(cat) %>">Look at this cat</a>
    <%= render 'cat', cat: cat %>
    <br>
  </li>
  <% end %>
</ul>

